# Tailwind output directory. The compiled kura.css is generated by
# `npm run build:css` (invoked via `make build`) and is committed so a fresh
# `go build` on a host without Node.js can still produce a working binary.
# This file exists so the empty directory survives in git before the first
# Tailwind compile.
